{
  "name": "root",
  "version": "1.0.0",
  "scripts": {
    "start": "npm run build && npm run start-server",
    "build": "cd ./ui && npm install --force && npm run build",
    "start-server": "cd ./api && npm install && npm start"
  },
  "author": "",
  "license": "ISC",
  "description": ""
}
